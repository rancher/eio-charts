apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opdom.fullname" . }}-test-connection"
  labels:
    {{- include "opdom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opdom.fullname" . }}:{{ .Values.server.service.port }}']
  restartPolicy: Never
